# P2-B1-T1 Billing implementation evidence

Worktree: `/Users/user01/Music/SEMForge-worktrees/parallel-billing/semforge`

Base HEAD: `1ee11c5`

Scenario: Toss automatic billing state machine, HTTP adapter, PostgreSQL runtime store, route handlers, billing role, encrypted key + HMAC fingerprint, append-only ledger.

Implemented files:

- `src/server/billing/domain.ts`
- `src/server/billing/service.ts`
- `src/server/billing/toss-client.ts`
- `src/server/billing/http.ts`
- `src/server/billing/postgres-store.ts`
- `src/server/billing/runtime.ts`
- `src/app/api/v1/billing/authorize/route.ts`
- `src/app/api/v1/billing/payment-method/route.ts`
- `src/app/api/v1/billing/retry/route.ts`
- `src/app/api/v1/billing/cancel/route.ts`
- `src/app/api/v1/billing/subscription/route.ts`
- `src/app/api/v1/webhooks/toss/route.ts`
- `src/db/schema/core.ts`
- `src/db/migrations/0000_core.sql`

Validation commands and binary observables:

1. RED baseline
   - Invocation: `npm test -- src/server/billing/http.contract.test.ts`
   - Observable: failed with `Cannot find module '@/server/billing/http'`

2. Billing HTTP targeted GREEN
   - Invocation: `npm test -- src/server/billing/http.contract.test.ts`
   - Observable: exit code `0`, `tests 132`, `pass 132`, `fail 0`

3. Billing + DB targeted suite
   - Invocation: `npm test -- src/server/billing/domain.contract.test.ts src/server/billing/service.contract.test.ts src/server/billing/toss-client.contract.test.ts src/server/billing/http.contract.test.ts src/db/migrations/core.integration.test.ts src/db/schema/core.test.ts src/db/schema/client.test.ts src/lib/env.test.ts`
   - Observable: exit code `0`, `tests 133`, `pass 133`, `fail 0`

4. Full lint/typecheck/test
   - Invocation: `npm run lint && npm run typecheck && npm test`
   - Observable: exit code `0`, full test observable `tests 133`, `pass 133`, `fail 0`

5. Production build, audit, diff check
   - Invocation: `npm run build && npm audit --omit=dev && git status --short && git diff --check`
   - Observable: exit code `0`, Next build compiled successfully, route manifest includes the five billing routes and Toss webhook, `found 0 vulnerabilities`, `git diff --check` clean.

Coverage notes:

- First charge amount is fixed at `49_000` KRW in domain/service/schema checks.
- Retry schedule is fixed at `+1/+3/+5` days and grace at `7` days by contract tests.
- Toss v1 billing key issue, automatic charge, Query API reconciliation, timeout ambiguity, and secret redaction are covered by `toss-client.contract.test.ts` and `service.contract.test.ts`.
- HTTP billable POSTs require `Idempotency-Key`; request body `workspaceId` is rejected before service invocation.
- Webhook uses `tosspayments-webhook-transmission-id` for provider event dedupe and does not require session auth.
- PostgreSQL migration verifies `semforge_billing` has no `BYPASSRLS`, payment methods require encrypted billing key + HMAC fingerprint, and `billing_ledger_events` is append-only for the billing role.

Known limitation:

- The shell default `node` binary is `v25.4.0` in this worktree. Parent coordinator separately reported billing targeted Node 24 `26/26 PASS`; local package `engines` remains `>=24 <25`.
